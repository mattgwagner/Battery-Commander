@model IEnumerable<BatteryCommander.Common.Models.Soldier>

@{
    ViewBag.Title = "List";
}

<h2>@ViewBag.Title</h2>

<table class="table">
    <tr>
        <th>
            Soldier
        </th>
    </tr>

    @foreach (var soldier in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(m => soldier)
            </td>
        </tr>
    }
</table>