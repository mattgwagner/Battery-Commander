@model BatteryCommander.Common.Models.Group

@using BatteryCommander.Common;

@{
    Boolean use_short_name = Convert.ToBoolean(ViewData["UseShortName"] ?? false);
}

@Html.ActionLink(use_short_name ? Model.ShortName() : Model.DisplayName(), "List", "Soldier", new { groupId = (int)Model }, null)