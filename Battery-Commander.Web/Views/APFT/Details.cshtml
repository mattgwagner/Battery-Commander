@model APFT

@if (!Model.IsPassing)
{
    @Html.ActionLink("Generate Counseling", "Counseling", "APFT", new { Model.Id }, new { @class = "btn btn-default" })
}

<fieldset>
    <legend>APFT for @Html.DisplayFor(model => model.Soldier) on @Html.DisplayFor(model => model.Date)</legend>
</fieldset>